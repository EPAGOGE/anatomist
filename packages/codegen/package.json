{
  "name": "@epagoge/codegen",
  "version": "0.0.0",
  "private": true,
  "description": "Graph-to-code pipeline. Takes a GraphSpec (nodes, edges, properties) and produces working framework code via a pluggable backend. PyTorch is the only backend in Phase 0 sub-phase E; JAX/MLX/Triton land later without rewriting the pipeline.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./graph": "./src/graph/index.ts",
    "./backends/pytorch": "./src/backends/pytorch.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@epagoge/components": "^0.0.0",
    "@epagoge/shared": "^0.0.0",
    "zod": "^4.4.3"
  }
}
