# Base deps (always required)
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
pydantic-settings>=2.0
websockets>=12.0
msgpack>=1.0
numpy>=1.24
python-multipart>=0.0.6
httpx>=0.25

# ML deps (uncomment when ready to load real models — ~3-5 GB download)
# torch>=2.1
# transformer-lens>=2.0
# transformers>=4.40
# accelerate>=0.30

# SAE deps (uncomment for Gemma Scope SAE browsing — V2)
# sae-lens>=3.0

# Cloud runtime (uncomment for RunPod programmatic deploy — V2)
# runpod>=1.5
